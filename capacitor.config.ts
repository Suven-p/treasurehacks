import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fitnessflow.app',
  appName: 'treasurehacks',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
